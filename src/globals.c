#include "globals.h"

float deltaTime = 0.0f; // Ensure that deltaTime is initialized at the start of the program
float gravity = 600.0f;
float timePassed = 0.0f; // Track total time passed since the start of the game